# OKYπY Board-Pack — runtime deps for the launcher/server (run.bat / Launch.py /
# serve.py / generate.py). Lower-bound pins so new Python versions get prebuilt
# wheels (no compiler needed on user machines).
# Install:  pip install -r requirements.txt  &&  python -m playwright install chromium
openpyxl>=3.1.5
python-pptx>=1.0.2
Pillow>=11.0
playwright>=1.49
