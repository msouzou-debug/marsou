msal>=1.24
requests>=2.31
lxml>=4.9
pdfplumber>=0.10
openpyxl>=3.1
Flask>=3.0
waitress>=2.1
APScheduler>=3.10
PyYAML>=6.0
rapidfuzz>=3.0

# OCR path (Stage 2d) — also needs the tesseract-ocr binary with ell+eng data;
# without these installed, scanned PDFs route to the human review queue.
pytesseract>=0.3.10
pdf2image>=1.16
